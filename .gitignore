# macOS
.DS_Store

# Editor / IDE
.idea/
*.iml
.vscode/
*.swp
*.swo
*~

# loft build / cache (placeholder; refine when build pipeline lands)
target/
build/
dist/
.cache/

# Generated docs
doc/*.html

# Local test outputs
*.log
result.txt
/tmp/

# Golden-image tests: actual outputs are regenerated every run;
# only the goldens are checked in.
tests/actual/

# Interactive-editor save (E1-live writes to dryopea_save.json
# in the cwd).
dryopea_save.json
